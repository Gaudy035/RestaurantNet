import Position from '@/types/position';

interface LocationEmployeeData {
  userId: string;
  firstName: string;
  lastName: string;
  locationId: string;
  position: Position;
}

export default LocationEmployeeData;
